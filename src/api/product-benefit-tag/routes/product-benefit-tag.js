'use strict';

/**
 * product-benefit-tag router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-benefit-tag.product-benefit-tag');
