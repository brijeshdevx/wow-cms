{
  "collectionName": "components_head_countdown_timers",
  "info": {
    "displayName": "Countdown Timer",
    "icon": "clock",
    "description": ""
  },
  "options": {},
  "attributes": {
    "TimerType": {
      "type": "enumeration",
      "enum": [
        "Daily",
        "Custom"
      ],
      "default": "Custom",
      "required": true
    },
    "startTime": {
      "type": "time",
      "required": true,
      "default": "00:00"
    },
    "endTime": {
      "type": "time",
      "required": true,
      "default": "00:00"
    },
    "startDate": {
      "type": "date",
      "required": true
    },
    "endDate": {
      "type": "date",
      "required": true
    }
  }
}
